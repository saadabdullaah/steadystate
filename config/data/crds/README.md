# Phase 7 external CRDs

These exact CRDs are rendered from the frozen `cloudnative-pg` `0.29.0`
and `plugin-barman-cloud` `0.7.0` Helm charts. They are used only by
envtest and schema tests; the full GitOps profile installs the same CRDs
from the checksum-verified charts.
